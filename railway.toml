[build]
builder = "nixpacks"
buildCommand = "echo 'redirect-only — skipping React build'"

[deploy]
startCommand = "node redirect-server.cjs"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
